import {DotNetFrameworkBadge, DotNetCoreBadge} from "@site/src/components/CustomBadge";

# Installation

Consul.NET is available as a [NuGet package](https://www.nuget.org/packages/Consul/), it can be installed using the following command:

```shell
dotnet add package Consul
```

<p>
    <DotNetFrameworkBadge/>{` `}<DotNetCoreBadge/>
</p>
